<section class="main-hero section section--soft" id="main-hero">
    <div class="container main-hero__inner">
        <div class="main-hero__content">
            <div class="main-hero__badge">Поставки для бизнеса и предприятий</div>

            <h1>
                Оптовые поставки соли
                <span>для бизнеса, производства и складской логистики</span>
            </h1>

            <p class="main-hero__text">
                Пищевая, техническая и таблетированная соль, противогололедные реагенты.
                Работаем по договору, предоставляем документы и организуем доставку по регионам.
            </p>

            <div class="main-hero__actions">
                <a class="btn btn--primary" href="#contact">Оставить заявку</a>
                <a class="btn btn--secondary" href="#contact">Получить прайс</a>
            </div>

            <div class="main-hero__meta">
                <a href="tel:+70000000000" class="main-hero__phone">+7 (000) 000-00-00</a>
                <p>Быстрый расчёт стоимости, подбор продукции и консультация по поставке.</p>
            </div>

            <ul class="main-hero__features">
                <li>Оптовые цены</li>
                <li>Доставка по регионам</li>
                <li>Работа с юрлицами</li>
                <li>Документы и сертификаты</li>
            </ul>
        </div>

        <div class="main-hero__visual" aria-hidden="true">
            <div class="hero-panel hero-panel--large">
                <div class="hero-panel__label">Надёжный поставщик</div>
                <h2>Поставка под задачи клиента</h2>
                <p>
                    Подбираем тип продукции, объём, формат отгрузки и логистику
                    под конкретные потребности бизнеса.
                </p>
            </div>

            <div class="hero-panel hero-panel--small">
                <strong>Пищевая соль</strong>
                <span>для производства и торговли</span>
            </div>

            <div class="hero-panel hero-panel--small">
                <strong>Техническая соль</strong>
                <span>для промышленных и технических задач</span>
            </div>

            <div class="hero-panel hero-panel--small">
                <strong>Таблетированная соль</strong>
                <span>для систем водоочистки</span>
            </div>

            <div class="hero-panel hero-panel--small">
                <strong>Реагенты</strong>
                <span>для дорог и территорий</span>
            </div>
        </div>
    </div>
</section>

<style>
    .main-hero {
        position: relative;
        z-index: 10;
        margin-top: 0;
    }

    .main-hero__inner {
        display: grid;
        grid-template-columns: minmax(0, 1.1fr) minmax(320px, 0.9fr);
        gap: 40px;
        align-items: center;
    }

    .main-hero__content {
        max-width: 720px;
    }

    .main-hero__badge {
        display: inline-flex;
        padding: 8px 12px;
        border-radius: 999px;
        background: rgba(29, 78, 216, 0.08);
        color: var(--color-primary);
        font-size: 14px;
        font-weight: 700;
        margin-bottom: 18px;
    }

    .main-hero__content h1 {
        margin: 0 0 20px;
        font-size: clamp(40px, 5.6vw, 68px);
        line-height: 0.98;
        letter-spacing: -0.04em;
        color: var(--color-text);
    }

    .main-hero__content h1 span {
        display: block;
        margin-top: 12px;
        color: #334155;
        font-size: clamp(22px, 2.4vw, 32px);
        line-height: 1.15;
        font-weight: 600;
        letter-spacing: -0.02em;
    }

    .main-hero__text {
        margin: 0 0 28px;
        max-width: 640px;
        font-size: 18px;
        line-height: 1.75;
        color: var(--color-text-soft);
    }

    .main-hero__actions {
        display: flex;
        flex-wrap: wrap;
        gap: 14px;
        margin-bottom: 24px;
    }

    .main-hero__meta {
        margin-bottom: 24px;
    }

    .main-hero__phone {
        display: inline-block;
        margin-bottom: 8px;
        font-size: 24px;
        font-weight: 800;
        color: var(--color-text);
    }

    .main-hero__meta p {
        margin: 0;
        color: var(--color-text-muted);
        line-height: 1.6;
    }

    .main-hero__features {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        padding: 0;
        margin: 0;
        list-style: none;
    }

    .main-hero__features li {
        padding: 12px 16px;
        background: #ffffff;
        border: 1px solid var(--color-border);
        border-radius: 999px;
        font-weight: 600;
        color: #334155;
        box-shadow: var(--shadow-sm);
    }

    .main-hero__visual {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 16px;
        align-content: center;
    }

    .hero-panel {
        border-radius: var(--radius-lg);
        background: rgba(255, 255, 255, 0.88);
        border: 1px solid rgba(148, 163, 184, 0.18);
        box-shadow: var(--shadow-lg);
        backdrop-filter: blur(10px);
    }

    .hero-panel--large {
        grid-column: 1 / -1;
        min-height: 220px;
        padding: 28px;
        display: flex;
        flex-direction: column;
        justify-content: flex-end;
    }

    .hero-panel__label {
        display: inline-flex;
        align-self: flex-start;
        margin-bottom: 14px;
        padding: 7px 10px;
        border-radius: 999px;
        background: #dbeafe;
        color: #1e3a8a;
        font-size: 13px;
        font-weight: 700;
    }

    .hero-panel--large h2 {
        margin: 0 0 12px;
        font-size: 28px;
        line-height: 1.1;
        color: var(--color-text);
    }

    .hero-panel--large p {
        margin: 0;
        color: var(--color-text-soft);
        line-height: 1.7;
    }

    .hero-panel--small {
        min-height: 150px;
        padding: 22px;
        display: flex;
        flex-direction: column;
        justify-content: flex-end;
    }

    .hero-panel--small strong {
        margin-bottom: 8px;
        font-size: 20px;
        color: var(--color-text);
    }

    .hero-panel--small span {
        color: var(--color-text-muted);
        line-height: 1.5;
    }

    @media (max-width: 1100px) {
        .main-hero__inner {
            grid-template-columns: 1fr;
        }

        .main-hero__visual {
            grid-template-columns: 1fr 1fr;
        }
    }

    @media (max-width: 767px) {
        .main-hero__inner {
            gap: 28px;
        }

        .main-hero__content h1 {
            font-size: 40px;
        }

        .main-hero__content h1 span {
            font-size: 22px;
        }

        .main-hero__text {
            font-size: 16px;
        }

        .main-hero__phone {
            font-size: 20px;
        }

        .main-hero__features {
            gap: 10px;
        }

        .main-hero__features li {
            width: 100%;
            border-radius: 14px;
        }

        .main-hero__visual {
            grid-template-columns: 1fr;
        }

        .hero-panel--large {
            min-height: 180px;
        }
    }
</style>